# Project Mandates

## Environment Management
- **Conda Environment**: Always ensure the `WODLens` conda environment is activated before running any backend commands, tests, or scripts.
- **Backend Execution**: Prepend backend commands with `conda run -n WODLens` if a shell doesn't automatically inherit the environment.
